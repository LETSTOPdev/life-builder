# Copy this to .env.local and fill in your values

# Anthropic Claude API — enables real AI coaching
# Get your key at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Database (replace in-memory stores with a real DB)
# DATABASE_URL=postgresql://user:password@localhost:5432/buildr

# Auth secret (for JWT signing)
# AUTH_SECRET=your-32-char-random-secret-here

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
